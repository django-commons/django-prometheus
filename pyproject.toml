[build-system]
requires = ["setuptools >= 84.0.0, < 85.0.0", "wheel >= 0.48.0"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
addopts = " --ignore django_prometheus/tests/end2end"
